---
title: "<ID>: <Title>"
slug: /adr/<id-with-leading-zeros>-<slug>
status: proposed | accepted | superseded
date: YYYY-MM-DD
source_link: <link to the external source of truth, if one exists>
sync: body-managed
audience: [developer, agent]
---

:::info Source of truth
This page mirrors the decision record in <external system>, linked above. <External system> is
authoritative — if this page and the source disagree, the source wins and this page needs updating.
Remove this admonition entirely if there is no external source and this page IS the record.
:::

## What this binds in code

Plain-language summary of the concrete, checkable consequences: type/enum names, config keys,
class names — the part a reader needs without working through the full rationale below. If parts
of the decision aren't implemented yet, say so here explicitly (see the unwired-code pattern) —
don't let the "Decision" section below read as "and this is what happens today" when it isn't.

## Body (verbatim, original language, mirrors the source)

Copy-paste from the source system without rewriting — this is what lets a future automated sync
overwrite this section cleanly. If there's no external source, write directly in this section and
delete the "verbatim" framing.

### Context

### Decision

### Consequences
