# A linkdown br-* keeps its scope-link route in the main table and the kernel
# still uses it. Ignoring linkdown routes stops a stopped Docker bridge from
# shadowing CIn internal subnets / the VPN's virtual-IP range.
net.ipv4.conf.all.ignore_routes_with_linkdown = 1
