# Control your input devices
# See https://wiki.hypr.land/Configuring/Variables/#input
input {

  # kb_layout = br
  # kb_variant = abnt2
  # kb_options = grp:alts_toggle
  
  kb_layout = br,us
  kb_variant = abnt2, intl
  # kb_options = grp:shifts_toggle # Altera o layout pressionando ambos os Shifts

  
  follow_mouse = 0

  # Change speed of keyboard repeat
  repeat_rate = 40
  repeat_delay = 250

  # Start with numlock on by default
  numlock_by_default = true

  # Increase sensitivity for mouse/trackpad (default: 0)
  # sensitivity = 0.35

  # Turn off mouse acceleration (default: false)
  # force_no_accel = true

  touchpad {
    # Use natural (inverse) scrolling
    natural_scroll = true

    # Use two-finger clicks for right-click instead of lower-right corner
    clickfinger_behavior = true

    # Control the speed of your scrolling
    scroll_factor = 0.4

    # Enable the touchpad while typing
    # disable_while_typing = false

    # Left-click-and-drag with three fingers
    # drag_3fg = 1
  }
}

# Teclado americano (AnnePro2): sempre em us(altgr-intl), sem alternar
# layout. Cedilha: AltGr + , (dead_cedilla) depois c -> ç
# lv3:ralt_switch força o Alt direito físico a agir como Level3 Shift
# (AltGr de verdade); sem isso o firmware do AnnePro2 reporta Alt_R mas
# o xkb trata como Mod1 comum e os deadkeys do altgr-intl nunca ativam.
# device {
#     name = obins-obins-annepro2-keyboard
#     kb_layout = us
#     kb_variant = altgr-intl
#     kb_options = lv3:ralt_switch
# }
#
# device {
#     name = ite-tech.-inc.-ite-device(8910)-keyboard
#     kb_layout = br
#     kb_variant = abnt2
#     kb_options =
# }

# Scroll nicely in the terminal
windowrule = match:class (Alacritty|kitty|foot), scroll_touchpad 1.5
windowrule = match:class com.mitchellh.ghostty, scroll_touchpad 0.2

# Enable touchpad gestures for changing workspaces
# See https://wiki.hyprland.org/Configuring/Gestures/
gesture = 3, horizontal, workspace

# Enable touchpad gestures for moving focus (helpful on scrolling layout)
# gesture = 3, left,  dispatcher, movefocus, l
# gesture = 3, right, dispatcher, movefocus, r
