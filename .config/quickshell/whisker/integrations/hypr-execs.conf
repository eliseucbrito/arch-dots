### Whisker execs for Hyprland ###

# Executes Whisker on startup
exec-once = quickshell -p ~/.config/quickshell/whisker

# Required for clipboard history
exec-once = wl-paste --watch bash -c 'cliphist store && whisker ipc cliphist update'
